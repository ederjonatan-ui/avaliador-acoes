<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Avaliador de Ações - Mobile</title>

<!-- Plotly para o gráfico -->
<script src="https://cdn.plot.ly/plotly-latest.min.js"></script>

<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #0b0f19;
        color: white;
        padding: 10px;
    }

    h2 {
        margin-top: 20px;
        font-size: 18px;
        color: #9bb0ff;
    }

    #topo {
        display: flex;
        gap: 10px;
        margin-bottom: 15px;
    }

    input {
        flex: 1;
        padding: 12px;
        border-radius: 8px;
        border: none;
        font-size: 16px;
    }

    button {
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-size: 16px;
        cursor: pointer;
        font-weight: bold;
    }

    #btnAvaliar {
        background: #1a73e8;
        color: white;
    }

    #btnExecutar {
        background: #1a73e8;
        width: 100%;
        margin-top: 15px;
    }

    #btnDark {
        background: #444;
        width: 100%;
        margin-top: 10px;
    }

    #painel {
        margin-top: 20px;
        background: #111522;
        padding: 15px;
        border-radius: 12px;
    }

    .card {
        margin-bottom: 15px;
        padding-bottom: 10px;
        border-bottom: 1px solid #222a3d;
    }

    .timer {
        font-size: 22px;
        color: #ffcc00;
        font-weight: bold;
    }
</style>
</head>

<body>

<div id="topo">
    <input id="ticker" placeholder="Ex.: PETR4.SA">
    <button id="btnAvaliar" onclick="avaliar()">Avaliar</button>
</div>

<div id="chart" style="height: 300px;"></div>

<div id="painel">

    <div class="card">
        <h2>Recomendação</h2>
        <div id="resultado">---</div>
    </div>

    <div class="card">
        <h2>Ganho Simulado</h2>
        <div id="ganho">---</div>
    </div>

    <div class="card">
        <h2>Notícias Recentes</h2>
        <ul id="noticias"></ul>
    </div>

    <div class="card">
        <h2>Tempo Restante</h2>
        <div id="contador" class="timer">--</div>
    </div>

</div>

<button id="btnExecutar" onclick="executarOrdem()">Executar Ordem</button>
<button id="btnDark" onclick="toggleDark()">Modo Escuro</button>

<script>
let timer = null;
let recomendacao = null;

function toggleDark() {
    document.body.classList.toggle("dark");
}

async function avaliar() {
    const ticker = document.getElementById("ticker").value.trim();
    if (!ticker) return alert("Digite um ticker");

    // Busca dados reais do Yahoo Finance
    const url = `https://query1.finance.yahoo.com/v8/finance/chart/${ticker}?interval=5m&range=1d`;
    const resp = await fetch(url);
    const data = await resp.json();

    const result = data.chart.result[0];
    const timestamps = result.timestamp;
    const candles = result.indicators.quote[0];

    // Médias móveis
    const closes = candles.close;
    const mm20 = closes.slice(-20).reduce((a,b)=>a+b,0) / 20;
    const mm50 = closes.slice(-50).reduce((a,b)=>a+b,0) / 50;
    const ultimo = closes[closes.length - 1];

    if (ultimo > mm20 && mm20 > mm50) recomendacao = "COMPRA";
    else if (ultimo < mm20 && mm20 < mm50) recomendacao = "VENDA";
    else recomendacao = "MANTER";

    document.getElementById("resultado").innerHTML =
        `<b>${recomendacao}</b><br>Acerto: 68% | Erro: 32%`;

    // Simulação de ganho
    const ganho = 100 * (1 + Math.random() * 0.2);
    document.getElementById("ganho").innerHTML =
        `R$100
